extends: default

rules:
  document-start: disable
  indentation: disable
  line-length:
    max: 160
    level: warning
  empty-lines: disable
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
  truthy:
    check-keys: false

ignore: |
  .git/
  .pytest_cache/
